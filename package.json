{
  "scripts": {
    "cypress": "cypress run",
    "cy:parallel": "cypress-parallel -s cypress run -t 2 -d cypress/e2e",
    "cy:parallel:spec": "cypress-parallel -s cypress run --spec",
    "allure:generate": "npx allure generate allure-results --clean -o allure-report",
    "allure:single": "npx allure generate allure-results --clean --single-file -o allure-report",
    "allure:serve": "npx allure serve allure-results",
    "allure:open": "npx allure open allure-report"
  },
  "devDependencies": {
    "@cypress/grep": "^5.0.0",
    "@types/node": "^24.5.2",
    "allure-commandline": "^2.32.0",
    "allure-cypress": "^3.4.2",
    "cypress": "^15.7.1",
    "cypress-parallel": "^0.15.0",
    "puppeteer": "^24.31.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "typescript": "^5.9.2"
  }
}
